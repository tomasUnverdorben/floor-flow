apiVersion: v1
kind: Service
metadata:
  name: {{ include "floorflow.fullname" . }}
  labels:
    {{- include "floorflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "floorflow.selectorLabels" . | nindent 4 }}
